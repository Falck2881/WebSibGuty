"use strict"
//@ts-check

import {EntityDto} from "./Entities.js"

/**
 * Обновляет хранилище различными сущностями. 
 */
export class IAddModelIntoStorage
{
    /**
     * Преобразует массив json объектов в массив DTO моделей для конкретного Section(раздела)
     * @param {Array<EntityDto>} content
     */
    async execute(content)
    {

    }
}